apiVersion: v1
kind: Service
metadata:
  name: keywords-service
  namespace: microservice
spec:
  selector:
    app: keywords
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
